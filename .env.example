# Solana Token Creator Configuration

# Your wallet private key (as JSON array)
# Get this from Phantom: Settings > Export Private Key
PRIVATE_KEY=[1,2,3,4,5,...]

# Network: mainnet, devnet, or testnet
NETWORK=devnet

# RPC URL (optional - uses default if not set)
# RPC_URL=https://api.devnet.solana.com
